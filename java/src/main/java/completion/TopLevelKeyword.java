package completion;

/**
 * Top-level keyword completion test.
 * Covers: TC-20 (keyword completion at top level).
 * Based on EX-JV-4.
 */
// cla // <caret> TC-20: keyword class — expect 'class' keyword suggestion

public class TopLevelKeyword {
    // Placeholder class so the file compiles.
    // To test TC-20, place caret after "cla" in a blank line at the top level.
}
